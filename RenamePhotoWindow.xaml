<Window x:Class="PhotoRetouch.RenamePhotoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="파일 이름 바꾸기"
        Width="420"
        Height="170"
        MinWidth="360"
        MinHeight="160"
        Background="#171819"
        WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <SolidColorBrush x:Key="TextMain" Color="#F1F3F4"/>
        <SolidColorBrush x:Key="TextSub" Color="#B9C0C5"/>
        <SolidColorBrush x:Key="ControlBg" Color="#33373B"/>
        <SolidColorBrush x:Key="ControlStroke" Color="#555D64"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="Background" Value="{StaticResource ControlBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource ControlStroke}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="파일 이름 바꾸기" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>
        <TextBox x:Name="PhotoNameTextBox"
                 Grid.Row="1"
                 Height="32"
                 VerticalContentAlignment="Center"
                 Background="#202224"
                 Foreground="{StaticResource TextMain}"
                 BorderBrush="{StaticResource ControlStroke}"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="취소" Width="78" Margin="0,0,8,0" Click="CancelButton_Click"/>
            <Button Content="확인" Width="78" Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
